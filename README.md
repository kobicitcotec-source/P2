<!-- managed-project-readme -->
# P2

An independent test repository used to verify per-project Git isolation and Claude-managed project instructions.

The project currently has no application source code, dependencies, run command, or automated tests.
